Inject Private Dependencies

1) Add to the <team> folder private dependencies so Cloud9 does not need to access private repositories like Artifactory behind a VPN
2) Rename the <team> folder to the team folder name that would exist in Artifactory
